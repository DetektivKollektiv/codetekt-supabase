# Continuous WAL archiving for point-in-time recovery.
# Restore settings intentionally live only in the restore runbook.
archive_mode = on
archive_command = '/usr/local/bin/wal-g wal-push %p'
archive_timeout = 300

# This production instance is not a read replica.
hot_standby = off
